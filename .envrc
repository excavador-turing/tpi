# `--features native-tls` links OpenSSL through reqwest, which is how the
# on-board build talks to the daemon's self-signed certificate. Without
# pkg-config and the OpenSSL dev output on PATH, that feature fails to build
# here while CI -- whose runner image happens to carry both -- stays green.
eval "$(devbox generate direnv --print-envrc)"
